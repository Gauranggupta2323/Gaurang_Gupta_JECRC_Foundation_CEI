make a virtual environment in your system to make this code run smoothly in VS Code
or you can directly run them by installing required pipelines in Google Colab

pip install "pycaret[full]"

 python.exe -m pip install --upgrade pip

pip install -r requirements.txt